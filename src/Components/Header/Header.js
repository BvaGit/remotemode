import React from 'react';
import {Navbar, Nav, Container} from 'react-bootstrap';

function header() {
    return (
        <header className="header">
            <Container>
                <a href="/">SHARETRADE<span>.COM</span></a>
            </Container>
        </header>

    );
};

export default header;